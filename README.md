# Compressors
A collection of lossless compression algorithms implemented in C++.

## Testing
To run the test suite:
```bash
mkdir build && cd build
cmake ..
make
./run_tests
```

As it stands, this runs all implementations in `compressor_impls/` against all test files in `inputs/`. Future work will ideally update this to target certain implementations.

## Benchmarking
TODO
